---
interface Props {
    title: string;
    eyebrow: string;
    desc: string;
    image: string;
    href?: string;
    featured?: boolean;
}

const { title, eyebrow, desc, image, href, featured = false } = Astro.props;
---

<article class:list={["bento-card", "reveal", featured && "bento-card-featured"]}>
    <div class="bento-image">
        <img src={image} alt={`${title} 제작 샘플`} loading="lazy" />
    </div>
    <div class="bento-content">
        <p class="eyebrow">{eyebrow}</p>
        <h3>{title}</h3>
        <p>{desc}</p>
    </div>
    {href && <a href={href} class="bento-link" aria-label={`${title} 자세히 보기`}>자세히 보기</a>}
</article>

<style>
    .bento-card {
        position: relative;
        min-height: 100%;
        display: flex;
        flex-direction: column;
        padding: var(--space-lg);
        border: 1px solid rgba(13, 27, 42, 0.06);
        border-radius: var(--radius-lg);
        background: var(--color-surface);
        box-shadow: var(--shadow-soft);
        overflow: hidden;
        transition: var(--transition-premium);
    }

    .bento-card::before {
        position: absolute;
        inset: -1px;
        border-radius: inherit;
        background: radial-gradient(circle at 24% 0%, rgba(217, 83, 15, 0.18), transparent 34%);
        content: "";
        opacity: 0;
        transition: var(--transition-premium);
        pointer-events: none;
    }

    .bento-card:hover {
        border-color: rgba(217, 83, 15, 0.22);
        transform: translateY(-8px);
        box-shadow: 0 28px 70px rgba(217, 83, 15, 0.15), 0 18px 46px rgba(13, 27, 42, 0.08);
    }

    .bento-card:hover::before {
        opacity: 1;
    }

    .bento-card-featured {
        grid-column: span 2;
        grid-row: span 2;
        background:
            linear-gradient(145deg, rgba(255, 255, 255, 0.96), rgba(255, 247, 242, 0.9)),
            var(--color-surface);
        box-shadow: 0 24px 70px rgba(13, 27, 42, 0.08);
    }

    .bento-image {
        position: relative;
        min-height: 150px;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: var(--space-lg);
        overflow: hidden;
        border-radius: calc(var(--radius-lg) - 6px);
        background: linear-gradient(145deg, rgba(13, 27, 42, 0.035), rgba(217, 83, 15, 0.035));
    }

    .bento-card-featured .bento-image {
        min-height: 330px;
    }

    .bento-image img {
        width: 100%;
        max-height: 280px;
        object-fit: contain;
        filter: drop-shadow(0 18px 26px rgba(0, 0, 0, 0.08));
        transition: var(--transition-premium);
    }

    .bento-card:hover .bento-image img {
        transform: scale(1.06);
    }

    .bento-card-featured .bento-image img {
        max-height: 360px;
    }

    .bento-content {
        position: relative;
        z-index: 1;
        margin-top: auto;
    }

    .bento-content h3 {
        margin: var(--space-sm) 0 0;
        color: var(--text-heading);
        font-size: 1.55rem;
        font-weight: 900;
        line-height: 1.2;
        word-break: keep-all;
    }

    .bento-card-featured .bento-content h3 {
        font-size: 3rem;
    }

    .bento-content p:not(.eyebrow) {
        margin: var(--space-sm) 0 0;
        color: var(--text-body);
        word-break: keep-all;
    }

    .bento-link {
        position: relative;
        z-index: 1;
        display: inline-flex;
        min-height: 48px;
        align-items: center;
        justify-content: center;
        margin-top: var(--space-lg);
        padding: 12px 18px;
        border-radius: var(--radius-md);
        background: var(--text-heading);
        color: #fff;
        font-weight: 900;
        text-decoration: none;
        transition: var(--transition-premium);
    }

    .bento-link:hover {
        background: var(--color-primary);
        transform: translateY(-3px);
        box-shadow: var(--shadow-hover);
    }

    @media (max-width: 768px) {
        .bento-card-featured {
            grid-column: span 1;
            grid-row: span 1;
        }

        .bento-card,
        .bento-card-featured {
            padding: 24px;
        }

        .bento-card:hover {
            transform: translateY(-4px);
        }

        .bento-card-featured .bento-image {
            min-height: 220px;
        }

        .bento-card-featured .bento-content h3 {
            font-size: 2.25rem;
        }
    }
</style>
